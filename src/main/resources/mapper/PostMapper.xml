<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Post">
  <sql id="visible">p.deleted_at IS NULL <if test="authorId != null">AND p.author_id = #{authorId}</if></sql>
  <sql id="columns">p.id, p.title, p.content, p.author_id, u.display_name AS author_name, p.created_at, p.updated_at, p.deleted_at</sql>
  <select id="find" resultType="egovframework.backoffice.mvp.post.Post">
    SELECT <include refid="columns"/> FROM posts p JOIN users u ON u.id = p.author_id
    WHERE p.id = #{value} AND p.deleted_at IS NULL
  </select>
  <select id="list" resultType="egovframework.backoffice.mvp.post.Post">
    SELECT <include refid="columns"/> FROM posts p JOIN users u ON u.id = p.author_id
    WHERE <include refid="visible"/> ORDER BY p.id DESC LIMIT #{limit} OFFSET #{offset}
  </select>
  <select id="count" resultType="long">SELECT COUNT(*) FROM posts p WHERE <include refid="visible"/></select>
  <insert id="create" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
    INSERT INTO posts (title, content, author_id) VALUES (#{title}, #{content}, #{authorId})
  </insert>
  <update id="edit">
    UPDATE posts SET title = #{title}, content = #{content}, updated_at = CURRENT_TIMESTAMP
    WHERE id = #{id} AND deleted_at IS NULL <if test="authorId != null">AND author_id = #{authorId}</if>
  </update>
  <update id="softDelete">
    UPDATE posts SET deleted_at = CURRENT_TIMESTAMP, updated_at = CURRENT_TIMESTAMP
    WHERE id = #{id} AND deleted_at IS NULL <if test="authorId != null">AND author_id = #{authorId}</if>
  </update>
</mapper>

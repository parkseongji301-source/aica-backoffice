<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Account">
  <sql id="columns">id, email, display_name, password_hash, role, active, password_change_required, auth_version, created_at, updated_at</sql>
  <select id="findByEmail" resultType="egovframework.backoffice.mvp.account.Account">
    SELECT <include refid="columns"/> FROM users WHERE email = #{value}
  </select>
  <select id="findById" resultType="egovframework.backoffice.mvp.account.Account">
    SELECT <include refid="columns"/> FROM users WHERE id = #{value}
  </select>
  <select id="findAll" resultType="egovframework.backoffice.mvp.account.Account">
    SELECT <include refid="columns"/> FROM users ORDER BY id
  </select>
  <select id="count" resultType="int">SELECT COUNT(*) FROM users</select>
  <select id="activeSuperCount" resultType="int">SELECT COUNT(*) FROM users WHERE active = TRUE AND role = 'SUPER_ADMIN'</select>
  <select id="lockChanges" resultType="int">SELECT id FROM account_guard WHERE id = 1 FOR UPDATE</select>
  <insert id="create" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
    INSERT INTO users (email, display_name, password_hash, role)
    VALUES (#{email}, #{displayName}, #{passwordHash}, #{role})
  </insert>
  <update id="changePassword">
    UPDATE users SET password_hash = #{hash}, password_change_required = #{required},
      auth_version = auth_version + 1, updated_at = CURRENT_TIMESTAMP WHERE id = #{id}
  </update>
  <update id="changeRole">
    UPDATE users SET role = #{role}, auth_version = auth_version + 1, updated_at = CURRENT_TIMESTAMP WHERE id = #{id}
  </update>
  <update id="deactivate">
    UPDATE users SET active = FALSE, auth_version = auth_version + 1, updated_at = CURRENT_TIMESTAMP WHERE id = #{value}
  </update>
</mapper>
